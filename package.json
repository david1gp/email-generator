{
  "name": "@adaptive-sm/email-generator",
  "version": "0.5.0",
  "scripts": {
    "dev": "email dev --dir=./src/templates",
    "dev:worker": "wrangler dev",
    "start": "bun ./src/server/server.ts",
    "deploy": "wrangler deploy --var VERSION:`bun run getVersion`",
    "getVersion": "bun run src/ops/printPackageVersion.ts",
    "generateExports": "bun run src/generateExports.ts",
    "generateBarrel": "bun run src/generateBarrelImports.ts",
    "build": "rm -rf ./dist && bun tsc -p ./tsconfig.lib.json",
    "tail": "wrangler tail email-generator-worker",
    "test": "bun test",
    "test:w": "bun test --watch",
    "release": "bash ./ops/release.sh",
    "update": "bun x npm-check-updates -u && bun i",
    "update:commit": "bun x npm-check-updates -u && bun i && git add package.json && bun run tsc && git commit -m \"chore(deps): update dependencies\" && git push",
    "clean": "rm -rf ./dist",
    "reset": "rm -rf ./node_modules"
  },
  "dependencies": {
    "@adaptive-sm/utils": "^0.4.2",
    "valibot": "^1.1.0"
  },
  "devDependencies": {
    "@react-email/components": "^0.5.7",
    "@react-email/preview-server": "^4.3.1",
    "@react-email/render": "^1.4.0",
    "@types/bun": "latest",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-email": "^4.3.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.44.0"
  },
  "prettier": {
    "semi": false,
    "printWidth": 120,
    "trailingComma": "all"
  },
  "type": "module",
  "private": false,
  "license": "MIT",
  "homepage": "https://github.com/adaptive-shield-matrix/email-generator",
  "description": "A lightweight, self-hostable service for rendering HTML emails with React Email",
  "keywords": [
    "email",
    "templates",
    "react-email",
    "html-email",
    "serverless",
    "cloudflare-workers",
    "bun",
    "typescript"
  ],
  "repository": {
    "url": "git+https://github.com/adaptive-shield-matrix/email-generator.git"
  },
  "exports": {
    "./*": "./dist/*"
  },
  "files": [
    "dist"
  ]
}
